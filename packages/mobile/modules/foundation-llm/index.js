// Stub — the module is consumed via NativeModules.FoundationLLM in
// `src/shared/ai/backends/foundation.ts`. This file exists so
// `pod 'FoundationLLM', :path => '../modules/foundation-llm'` resolves
// via the Node-style package layout (CocoaPods reads the package.json
// alongside).
module.exports = {};
